langcode: en
status: true
dependencies:
  module:
    - menu_ui
    - scheduler
third_party_settings:
  scheduler:
    expand_fieldset: when_required
    fields_display_mode: vertical_tab
    publish_enable: true
    publish_past_date: publish
    publish_past_date_created: false
    publish_required: false
    publish_revision: false
    publish_touch: false
    show_message_after_update: true
    unpublish_enable: true
    unpublish_required: false
    unpublish_revision: false
  menu_ui:
    available_menus:
      - main
    parent: 'main:'
name: 'Webform page'
type: webform
description: "<em>Webform pages</em> allow you to use the same webform multiple times and keep results separate - e.g., a volunteer signup form used each year. Results for <strong>EACH</strong> year are on the Results tab of the webform page (go to Content).<br>\r\n"
help: "Click \"Menu settings\" at left to provide a menu link to the page, or link to this page from another page or block.<br>\r\nScheduling publication is optional.\r\n"
new_revision: true
preview_mode: 1
display_submitted: false
